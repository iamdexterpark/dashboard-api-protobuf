/*
  Meraki Dashboard API

  A RESTful API to programmatically manage and monitor Cisco Meraki networks at scale.  > Date: 04 October, 2023 > > [Recent Updates](https://meraki.io/whats-new/)  ---  [API Documentation](https://meraki.io/api)  [Community Support](https://meraki.io/community)  [Meraki Homepage](https://www.meraki.com) 

  The version of the OpenAPI document: 1.38.0

  Generated by OpenAPI Generator: https://openapi-generator.tech
*/

syntax = "proto3";

package openapitools;

import public "models/update_device_switch_routing_interface_dhcp_request_dhcp_options_inner.proto";
import public "models/update_device_switch_routing_interface_dhcp_request_fixed_ip_assignments_inner.proto";
import public "models/update_device_switch_routing_interface_dhcp_request_reserved_ip_ranges_inner.proto";

message UpdateDeviceSwitchRoutingInterfaceDhcpRequest {

  // The DHCP mode options for the switch interface        ('dhcpDisabled', 'dhcpRelay' or 'dhcpServer')
  enum DhcpModeEnum {
    DhcpModeEnum_DHCPDISABLED = 0;
    DhcpModeEnum_DHCPRELAY = 1;
    DhcpModeEnum_DHCPSERVER = 2;
  }

  DhcpModeEnum dhcpMode = 524528333;

  // The DHCP relay server IPs to which DHCP packets would get relayed for the switch interface
  repeated string dhcpRelayServerIps = 404689065;

  // The DHCP lease time config for the dhcp server running on switch interface         ('30 minutes', '1 hour', '4 hours', '12 hours', '1 day' or '1 week')
  enum DhcpLeaseTimeEnum {
    DhcpLeaseTimeEnum__1_DAY = 0;
    DhcpLeaseTimeEnum__1_HOUR = 1;
    DhcpLeaseTimeEnum__1_WEEK = 2;
    DhcpLeaseTimeEnum__12_HOURS = 3;
    DhcpLeaseTimeEnum__30_MINUTES = 4;
    DhcpLeaseTimeEnum__4_HOURS = 5;
  }

  DhcpLeaseTimeEnum dhcpLeaseTime = 180039853;

  // The DHCP name server option for the dhcp server running on the switch interface         ('googlePublicDns', 'openDns' or 'custom')
  enum DnsNameserversOptionEnum {
    DnsNameserversOptionEnum_CUSTOM = 0;
    DnsNameserversOptionEnum_GOOGLEPUBLICDNS = 1;
    DnsNameserversOptionEnum_OPENDNS = 2;
  }

  DnsNameserversOptionEnum dnsNameserversOption = 93351089;

  // The DHCP name server IPs when DHCP name server option is         'custom'
  repeated string dnsCustomNameservers = 72952397;

  // Enable DHCP boot options to provide PXE boot options configs for the dhcp server running on the switch         interface
  bool bootOptionsEnabled = 387283373;

  // The PXE boot server IP for the DHCP server running on the switch interface
  string bootNextServer = 100272985;

  // The PXE boot server filename for the DHCP server running on the switch interface
  string bootFileName = 335893308;

  // Array of DHCP options consisting of code, type and value for the DHCP server running on the switch interface
  repeated UpdateDeviceSwitchRoutingInterfaceDhcpRequestDhcpOptionsInner dhcpOptions = 149255535;

  // Array of DHCP reserved IP assignments for the DHCP server running on the switch interface
  repeated UpdateDeviceSwitchRoutingInterfaceDhcpRequestReservedIpRangesInner reservedIpRanges = 108754470;

  // Array of DHCP fixed IP assignments for the DHCP server running on the switch interface
  repeated UpdateDeviceSwitchRoutingInterfaceDhcpRequestFixedIpAssignmentsInner fixedIpAssignments = 14416757;

}
